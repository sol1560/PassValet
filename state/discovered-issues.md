# 待验证问题

- dev身份绕过、dev命令和测试服务器不可用于发布版，已在ARM64与Intel测试包验证。
- 其他观察先写复现与证据再归类，不凭代码存在宣称通过。

## 已复现并修复

- 创建参数touchid_keychain与后端默认JSON名称touch_id_keychain不一致，真实macOS截图复现；后端显式名称兼容存储/UI，Rust回归先失败后通过，真实创建和恢复码确认通过。
- Setup提前卸载恢复码页面已修复；34941386442的真实创建/恢复确认通过，截图已遮码并检查。
- IPC bind会删除普通文件，Drop会删除路径上的替换文件；新测试先失败，修复后通过。
- 丢弃客户端接收端不能结束空闲peer；缺换行的超大消息无限增长。补1MiB上限及接收端关闭检测，检查上限两侧。
- CLI只更新第一处同名env变量，后面的旧值仍有效；无效UTF-8被当空文件覆盖；写入不是原子操作且权限设置晚于写入；换行没有转义。新增4项回归先失败后通过。
- Keys隐藏与锁定清除明文经真实macOS验证，两次基础运行及后续9项运行均通过。
- rebind先覆盖旧解锁密钥，数据库写入失败后无法解锁：34947040427复现；改为每个kek_salt独立条目后34947480645验证故障、成功、旧版兼容通过。真实Keychain/指纹仍未验收。
- 扩展事件已限制当前连接，34948658354复现普通连接未被拒绝，34949091189修复验证通过。ext.hello仍不构成可信进程身份，不能宣传为阻止同用户恶意进程。
- 发布脚本忽略zip失败、缺产物仍退出0，脚本测试先2失败1通过，修复后3通过。真实包另在34950601179构建验证。
- 原始浏览器图片能进入模型请求：runner_safety先复现，移除截图工具后仍复现其他工具夹带图片，再从发送处去除图片后通过。保留文字操作，不承诺支持只能看图的页面；文字脱敏的覆盖范围仍待M4检查。
- NeedUser发出后立即继续会丢失通知并永久等待；容量1事件通道确定性复现。先注册等待再发布事件，额外检查已取消状态，立即继续与取消均通过。本地34项Rust通过，3项真实模型未运行。
- 采集格式错误被误报成功和覆盖原值：agent回归先失败再通过；VaultSink改为保存前拒绝，34952976122的真实macOS内存库测试验证原值保留。未请求类型也不再交给扩展或存储。
- env多行引号中的同名文本被误改、旧值尾部残留：回归先失败后7项CLI通过；未闭合引号拒绝写入。
- 网页标题/网址未经过正文脱敏：模型请求回归先失败，改为拼接后统一脱敏，通过。
- 可移植crate的严格clippy问题已修复；CLI部分取值失败退出码与并发目录锁已经macOS验证，详见progress.md，勿重复当成未修复问题。
- 设置卡片溢出已由34954128893的DOM断言和截图检查验证修复，正式包已能显示页面。
- 扩展任务现限制起始网站，并校验读取/点击/导航前后；复制脚本只在该网站安装，去掉系统剪贴板兜底。单元测试与真实正常采集通过；不声称杜绝快速跳转竞争。
- 模型错误UTF-8截断崩溃、错误回显已知密钥、等待模型时无法及时中止、单批工具超过上限均已复现并修复，最新macOS用例通过。
- Cursor损坏配置不再被清空，重复安装保留其他服务、原子替换与0600已通过macOS测试。
- Codex首次安装漏写、重复安装不更新路径已复现；改用真实Codex CLI，含特殊路径与损坏配置保护的检查已在macOS通过。
- 默认备用模型拒绝temperature参数；移除硬编码参数后4项真实工具调用通过。设置连接测试16输出额度可能无文本，512额度下两种接口均有回复。
- 轮换Partial被当作Rotated返回旧密钥已由34962867609复现，34963420759验证修复；这只修正结果，不代表安全撤销已实现。
- 设置写入失败后内存配置仍改变已由34963420759复现；34964063115验证保存成功后再切换，以及临时文件替换、0600和失败清理。
- 等待用户时继续按钮消失已由34966972222复现，34967711151验证修复，等待界面截图已检查。
- 捕获结果晚于中止仍保存、用户取消Chrome调试却被自动重新接管均已由本地回归复现并修复；macOS正常采集回归在34969135783验证。

## 仍待处理，不算完成

- macOS完整workspace严格clippy与全库格式检查已通过；34964063115的Intel与ARM64包构建启动均通过。
- 自动轮换已在Runner入口暂时拒绝，防止通用点击误删。仍须实现程序层面的“先保存新key再撤销精确旧key”，不能将停用危险操作当作轮换完成。
- Stripe轮换剧本要求先让旧key立即过期再捕获新key，与安全顺序矛盾；OpenAI按通用名称passvalet识别旧key也不足以唯一匹配。没有精确旧key身份时应停止撤销。
- 元素读取与一次性复制采集已通过真实扩展验证；二次验证、提示注入、断线恢复和七服务真实账号仍有未验收项。
- Claude真实CLI用户范围注册与重复安装、真实删除确认均已在macOS通过。设置页/CLI配置片段的新路径转义待34969135783验证。
